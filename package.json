{
  "name": "jammming",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "git-scripts": "^0.2.1",
    "surge": "^0.19.0"
  },
  "git": {
    "scripts": {
      "pre-push": "surge --project ./build --domain http://jammming-by-meliorah.surge.sh/"
    }
  }
}
